/**
 * Main function. Builds first Scene with three.js
 */
function main() {
    let scene = new THREE.Scene();
    let camera = new THREE.PerspectiveCamera();
}
/*
Execute main function as start.
 */
main();
